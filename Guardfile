# A sample Guardfile
# More info at https://github.com/guard/guard#readme
# guard :shell do
#   watch('slides.md') { `keydown slides slides.md` }
# end


guard 'livereload' do
  watch('slides.md') { `keydown slides slides.md` }
end